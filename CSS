/* Variables de Color (Inspiradas en el moodboard adjunto) */
:root {
    --bg-color: #F4F1ED; /* Beige suave / Arena */
    --bg-light: #EAE6DE; /* Beige ligeramente más oscuro */
    --primary-color: #728069; /* Verde Salvia */
    --primary-dark: #56614f; 
    --text-dark: #3A3532; /* Marrón oscuro casi negro */
    --text-light: #7E7771; /* Gris cálido */
    --accent-color: #A97C73; /* Terracota / Rosa empolvado */
    
    --font-heading: 'Playfair Display', serif;
    --font-body: 'Montserrat', sans-serif;
}

/* Reset y Base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: var(--font-body);
    background-color: var(--bg-color);
    color: var(--text-dark);
    line-height: 1.6;
    overflow-x: hidden;
}

h1, h2, h3, .logo {
    font-family: var(--font-heading);
    font-weight: 400;
    color: var(--text-dark);
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

.section-padding {
    padding: 100px 0;
}

.text-center {
    text-align: center;
}

/* Tipografía específica */
h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    margin-bottom: 20px;
    font-style: italic;
}

h2 {
    font-size: 2.5rem;
    margin-bottom: 15px;
}

p {
    font-size: 1rem;
    color: var(--text-light);
}

/* Botones */
.btn {
    display: inline-block;
    padding: 12px 28px;
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 1px;
    text-transform: uppercase;
    transition: all 0.3s ease;
    border-radius: 2px; /* Ligeramente cuadrado para toque elegante */
    cursor: pointer;
}

.btn-primary {
    background-color: var(--primary-color);
    color: #fff;
    border: 1px solid var(--primary-color);
}

.btn-primary:hover {
    background-color: var(--primary-dark);
}

.btn-secondary {
    background-color: transparent;
    color: var(--text-dark);
    border: 1px solid var(--text-dark);
}

.btn-secondary:hover {
    background-color: var(--text-dark);
    color: var(--bg-color);
}

.btn-outline {
    background: transparent;
    color: var(--primary-color);
    border: 1px solid var(--primary-color);
}

.btn-outline:hover {
    background-color: var(--primary-color);
    color: #fff;
}

/* Navegación */
.navbar {
    padding: 25px 0;
    position: fixed;
    width: 100%;
    top: 0;
    background-color: rgba(244, 241, 237, 0.95);
    backdrop-filter: blur(10px);
    z-index: 1000;
    border-bottom: 1px solid rgba(0,0,0,0.05);
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.8rem;
    text-decoration: none;
    font-weight: 600;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 30px;
}

.nav-links a {
    text-decoration: none;
    color: var(--text-dark);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.3s ease;
}

.nav-links a:hover {
    color: var(--accent-color);
}

.menu-toggle {
    display: none;
    flex-direction: column;
    cursor: pointer;
}

.menu-toggle .bar {
    height: 2px;
    width: 25px;
    background-color: var(--text-dark);
    margin: 4px 0;
}

/* Hero Section */
.hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 80px;
}

.hero-content {
    max-width: 800px;
}

.hero p {
    font-size: 1.2rem;
    margin-bottom: 40px;
    max-width: 600px;
}

.hero-buttons {
    display: flex;
    gap: 20px;
}

/* Secciones Generales */
.section-header {
    text-align: center;
    margin-bottom: 60px;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
}

.bg-light {
    background-color: var(--bg-light);
}

/* Grids */
.grid {
    display: grid;
    gap: 40px;
}

.cards-grid {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.sectors-grid {
    grid-template-columns: repeat(2, 1fr);
}

/* Cards */
.card {
    background-color: #fff;
    padding: 50px 40px;
    border: 1px solid rgba(0,0,0,0.05);
    transition: transform 0.4s ease, box-shadow 0.4s ease;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0,0,0,0.04);
}

.card-icon {
    font-family: var(--font-heading);
    font-size: 2rem;
    color: var(--accent-color);
    margin-bottom: 20px;
}

.card h3 {
    margin-bottom: 15px;
    font-size: 1.4rem;
}

.sector-box {
    background-color: var(--bg-color);
    padding: 60px;
    border-left: 3px solid var(--primary-color);
}

.sector-box h3 {
    font-size: 2rem;
    margin-bottom: 20px;
    color: var(--primary-color);
}

/* CTA & Forms */
.cta {
    background-color: var(--primary-dark);
    color: #fff;
    text-align: center;
}

.cta h2, .cta p {
    color: #fff;
}

.cta p {
    opacity: 0.8;
    margin-bottom: 40px;
}

.contact-form {
    display: flex;
    justify-content: center;
    gap: 10px;
    max-width: 500px;
    margin: 0 auto;
}

.contact-form input {
    padding: 15px 20px;
    width: 100%;
    border: none;
    background-color: rgba(255,255,255,0.1);
    color: #fff;
    font-family: var(--font-body);
    outline: none;
}

.contact-form input::placeholder {
    color: rgba(255,255,255,0.6);
}

/* Footer */
footer {
    background-color: var(--text-dark);
    color: var(--bg-color);
    padding: 60px 0 20px;
}

.footer-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 40px;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    padding-bottom: 40px;
}

.footer-brand h4 {
    color: var(--bg-color);
    font-size: 1.5rem;
    margin-bottom: 10px;
}

.footer-brand p {
    color: rgba(255,255,255,0.6);
}

.footer-links {
    display: flex;
    gap: 20px;
}

.footer-links a {
    color: rgba(255,255,255,0.6);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.3s ease;
}

.footer-links a:hover {
    color: var(--accent-color);
}

.footer-bottom p {
    color: rgba(255,255,255,0.4);
    font-size: 0.8rem;
}

/* Animaciones */
.fade-in {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}

.fade-in.visible {
    opacity: 1;
    transform: translateY(0);
}

/* Responsive Design */
@media (max-width: 768px) {
    .nav-links, .navbar .btn-outline {
        display: none;
    }

    .menu-toggle {
        display: flex;
    }

    .hero-buttons {
        flex-direction: column;
    }

    .sectors-grid {
        grid-template-columns: 1fr;
    }

    .contact-form {
        flex-direction: column;
    }
    
    .sector-box {
        padding: 40px 20px;
    }

    .footer-container {
        flex-direction: column;
        text-align: center;
        gap: 30px;
    }
}
